<h1><%= @task.title  %></h1>
<p><%= @task.description %></p>

<%= link_to "Back Tasks", tasks_path %>
<%= link_to "Edit Task", edit_task_path(@task) %>
<%= link_to "Delete Task", task_path(@task), data:{turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
